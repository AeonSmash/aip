/** Hosted envelope board (HTTPS). Same bus as Unreal and the public CRT. */
export const BOARD_URL = 'https://arkavia-backend.vercel.app/aip/board'

export const SESSION = 'demo'

/** Must match preview-board.mjs AIP_WRITE_KEY_DCL. */
export const WRITE_KEY = 'dcl-demo'

/** Public CRT. Click the plaque after signal.terminal to throw the switch there. */
export const SWITCH_URL = 'https://aeonsmash.com/#aip-switch'

export const POLL_SECONDS = 2
